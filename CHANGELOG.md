# Журнал изменений

Хронология исправлений и улучшений системы. Формат: дата – раздел – описание.

## 2025-11-04
- Тесты/Инфраструктура: добавлен документ `QA_PROCESS.md` с итеративным процессом тестирования и исправлений.
- Безопасность/Загрузки: корректная обработка `HTTPException` в `/upload-logo` (400 для не-изображений не превращается в 500).
- API/Генерация: исправлена структура запроса к `/generate` (вложение в `invoice_data`).
- Производительность/Тесты: расширенный нагрузочный тест `/templates` с параметризацией (20/60 запросов, пороги 8.0s/18.0s).
- Интеграция: единый алиас `url` в ответах, добавлен `sha256` в ответы фонового сервиса.
- Предупреждения: рекомендация перейти на `httpx.ASGITransport` для снижения шума.

---

Запланировано:
- Логирование ключевых операций приложения и ошибок в `main.py`.
- Шаблон дефекта и чеклист ревью кода.
- Скрипт `run_all_checks.ps1` для запуска тестов и проверок.
